สถานะระบบเกมตอนนี้

ระบบเกมหลักในตอนนี้เป็นเกม 3D บน Next.js ที่ใช้ Three.js ร่วมกับ React Three Fiber และ Rapier Physics
- `GameScene` รับผิดชอบสร้างและจัดการ `Canvas`, `Camera`, `Light`, และ `Physics`
- แผนที่ยังเป็นแบบ Graybox ที่ประกอบด้วยพื้นและกำแพง/แพลตฟอร์มสำหรับการเคลื่อนที่
- ตัวละครหลักแยกเป็น `Player.tsx` สำหรับระบบเคลื่อนไหวและสถานะฟิสิกส์ และ `PlayerModel.tsx` สำหรับการแสดงผลและแอนิเมชัน

โครงสร้างโฟลเดอร์ในโปรเจกต์

- `src/app/` : โฟลเดอร์หลักของหน้าที่แสดงใน Next.js
  - `page.tsx` : หน้าแรกของแอปเกม
  - `layout.tsx` : กำหนดเลย์เอาต์พื้นฐานและ CSS global ของแอป
  - `globals.css` : สไตล์ทั่วไปสำหรับทั้งโปรเจกต์

- `src/components/game/` : โฟลเดอร์เก็บคอมโพเนนต์เกมหลัก
  - `GameScene.tsx` : สร้างระบบ Canvas, กล้อง, แสง, และ Physics รอบเกม
  - `Map.tsx` : สร้างพื้นและแพลตฟอร์มของแผนที่ (Graybox map)
  - `Player.tsx` : จัดการอินพุตคีย์บอร์ด, ฟิสิกส์ตัวละคร, และสถานะการเคลื่อนไหว
  - `PlayerModel.tsx` : โหลดและเรนเดอร์โมเดลตัวละครพร้อม AnimationMixer
  - `PushableBox.tsx` : วัตถุที่สามารถผลักได้ในเกม (หากมีการใช้งาน)

- `public/` : เก็บไฟล์สาธารณะสำหรับเว็บ
  - `public/player/` : เก็บไฟล์โมเดลหรือทรัพยากรตัวละคร
  - `public/map/` : เก็บทรัพยากรของแผนที่ เช่น พื้นผิวหรือข้อมูลการวาด

- ไฟล์คอนฟิกหลักของโปรเจกต์
  - `package.json` : กำหนด dependencies และสคริปต์ของโปรเจกต์
  - `tsconfig.json` : กำหนดค่า TypeScript
  - `next.config.ts` : การตั้งค่า Next.js
  - `eslint.config.mjs` : การตั้งค่า ESLint
  - `postcss.config.mjs` : การตั้งค่า PostCSS

สรุป

ตอนนี้ระบบเกมพร้อมสำหรับการพัฒนาโหมดเคลื่อนไหวและฟิสิกส์หลักของตัวละคร, มีโครงสร้างโฟลเดอร์แยกคอมโพเนนต์ตามหน้าที่, และยังมีพื้นที่สำหรับขยายแผนที่, โมเดล, และระบบอินพุตเพิ่มได้ในอนาคต.